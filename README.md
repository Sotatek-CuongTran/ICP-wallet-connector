 Run Project step by step
Prerequires: node >= 14, yarn
1. clone this project
2. go to root folder of this project
3. run `yarn install` command
4. run `yarn start` command and go to localhost:3000 to test
